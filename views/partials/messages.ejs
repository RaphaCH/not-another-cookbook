<% if(typeof errors!='undefined' ) { %>
  <% errors.forEach(function(error){ %>
    <p class="flashes-error">
      <%= error.msg %>
    </p>
    <% }) %>
      <% } %>

        <% if(success_msg!='' ) { %>
          <p class="flashes-success">
            <%=success_msg %>
          </p>
          <% } %>

            <% if(error_msg !='' ) { %>
              <p class="flashes-error">
                <%= error_msg %>
              </p>
              <% } %>
                <% if(error !='' ) { %>
                  <p class="flashes-error">
                    <%= error %>
                  </p>
                  <% } %>