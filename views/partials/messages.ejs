<% if(typeof errors!='undefined' ) { %>
  <% errors.forEach(function(error){ %>
    <p class="flash-error">
      <%= error.msg %>
    </p>
    <% }) %>
      <% } %>

        <% if(success_msg!='' ) { %>
          <p class="flash-success">
            <%=success_msg %>
          </p>
          <% } %>

            <% if(error_msg !='' ) { %>
              <p class="flash-error">
                <%= error_msg %>
              </p>
              <% } %>
                <% if(error !='' ) { %>
                  <p class="flash-error">
                    <%= error %>
                  </p>
                  <% } %>